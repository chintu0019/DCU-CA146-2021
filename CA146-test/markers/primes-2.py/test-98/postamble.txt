#!/usr/bin/env python

import sys

try:
   if len(a) <= i:
      sys.stderr.write("error: solution does not use linear search\n")
      sys.exit(1)
   else:
      print "ok"
except:
   ignore = 3
   print "ok"


